ALTER TABLE `call_log` ADD `service` TINYINT NOT NULL DEFAULT '1' COMMENT '1=>Airphone,2=>deepcall' AFTER `id`;
ALTER TABLE `call_log` CHANGE `service` `service` TINYINT NOT NULL DEFAULT '2' COMMENT '1=>Airphone,2=>deepcall';
ALTER TABLE `call_log` CHANGE `id` `id` INT NOT NULL AUTO_INCREMENT, CHANGE `request_vnm` `request_vnm` VARCHAR(100) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NULL DEFAULT NULL;

ALTER TABLE `call_log` ADD `did` VARCHAR(255) NULL DEFAULT NULL AFTER `lead_id`, ADD `c_type` VARCHAR(255) NULL DEFAULT NULL AFTER `did`, ADD `camp_id` VARCHAR(255) NULL DEFAULT NULL AFTER `c_type`, ADD `exc_adm` VARCHAR(255) NULL DEFAULT NULL AFTER `camp_id`, ADD `ivr_s_time` DATETIME NULL DEFAULT NULL AFTER `exc_adm`, ADD `ivr_e_time` DATETIME NULL DEFAULT NULL AFTER `ivr_s_time`, ADD `ivr_duration` INT NOT NULL AFTER `ivr_e_time`, ADD `service_user_id` INT NOT NULL AFTER `ivr_duration`, ADD `c_number` VARCHAR(255) NULL DEFAULT NULL AFTER `service_user_id`, ADD `master_num_ctc` VARCHAR(255) NULL DEFAULT NULL AFTER `c_number`, ADD `master_agent` VARCHAR(255) NULL DEFAULT NULL AFTER `master_num_ctc`, ADD `master_agent_number` VARCHAR(255) NOT NULL AFTER `master_agent`, ADD `master_group_id` VARCHAR(255) NOT NULL AFTER `master_agent_number`, ADD `talk_duration` INT NULL DEFAULT NULL AFTER `master_group_id`, ADD `agent_on_call_duration` INT NULL DEFAULT NULL AFTER `talk_duration`, ADD `call_id` VARCHAR(255) NULL DEFAULT NULL AFTER `agent_on_call_duration`, ADD `first_attended` VARCHAR(255) NULL DEFAULT NULL AFTER `call_id`, ADD `first_answer_time` DATETIME NULL DEFAULT NULL AFTER `first_attended`, ADD `last_hangup_time` DATETIME NULL DEFAULT NULL AFTER `first_answer_time`, ADD `last_first_duration` INT NOT NULL AFTER `last_hangup_time`, ADD `cust_answer_s_time` DATETIME NULL AFTER `last_first_duration`, ADD `cust_answer_e_time` DATETIME NULL AFTER `cust_answer_s_time`, ADD `cust_answer_duration` INT NOT NULL AFTER `cust_answer_e_time`, ADD `ivr_execute_flow` VARCHAR(255) NULL DEFAULT NULL AFTER `cust_answer_duration`, ADD `hangup_by_source_detected` INT NOT NULL AFTER `ivr_execute_flow`, ADD `forward` BOOLEAN NOT NULL DEFAULT FALSE AFTER `hangup_by_source_detected`, ADD `total_hold_duration` INT NOT NULL AFTER `forward`;

CREATE TABLE `prod_witw`.`call_log_numbers_details` ( `id` INT NOT NULL AUTO_INCREMENT , `call_log_id` INT NOT NULL , `ctc` VARCHAR(255) NOT NULL , `status` VARCHAR(255) NOT NULL , `ping` VARCHAR(255) NOT NULL , `number` VARCHAR(255) NOT NULL , `visit_id` INT NOT NULL , `node_id` VARCHAR(255) NOT NULL , `total_ring_duration` INT NOT NULL , `total_hold_duration` INT NOT NULL , `talk_duration` INT NULL DEFAULT NULL , `talk_s_time` DATETIME NULL DEFAULT NULL , `talk_e_time` DATETIME NULL DEFAULT NULL , `answer_s_time` INT NULL DEFAULT NULL , `answer_e_time` INT NULL DEFAULT NULL , `answer_duration` INT NULL DEFAULT NULL , `cli` VARCHAR(255) NOT NULL , `retry` INT NOT NULL , `recording_url` VARCHAR(255) NULL DEFAULT NULL , `created_at` INT NOT NULL , `created_updated` INT NOT NULL , PRIMARY KEY (`id`)) ENGINE = InnoDB;


ALTER TABLE `call_log` CHANGE `master_agent_number` `master_agent_number` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NULL DEFAULT NULL;

ALTER TABLE `call_log_numbers_details` CHANGE `created_updated` `updated_at` INT NOT NULL;

ALTER TABLE `call_log_numbers_details` CHANGE `visit_id` `visit_id` VARCHAR(255) NOT NULL;

ALTER TABLE `call_log` CHANGE `ivr_duration` `ivr_duration` INT NULL DEFAULT NULL;

ALTER TABLE `call_log` CHANGE `service_user_id` `service_user_id` INT NULL DEFAULT NULL, CHANGE `master_group_id` `master_group_id` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NULL DEFAULT NULL, CHANGE `last_first_duration` `last_first_duration` INT NULL DEFAULT NULL, CHANGE `cust_answer_duration` `cust_answer_duration` INT NULL DEFAULT NULL, CHANGE `hangup_by_source_detected` `hangup_by_source_detected` INT NULL DEFAULT NULL, CHANGE `total_hold_duration` `total_hold_duration` INT NULL DEFAULT NULL;